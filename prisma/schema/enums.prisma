// User Role Enum
enum Role {
  ADMIN
  SELLER
  CUSTOMER
}

// Auth Provider Enum
enum AuthProvider {
  LOCAL
  GOOGLE
}

// OTP Type Enum
enum OtpType {
  VERIFY_EMAIL
  LOGIN
  RESET_PASSWORD
}

// Store Status Enum
enum StoreStatus {
  PENDING
  APPROVED
  SUSPENDED
  REJECTED
}
